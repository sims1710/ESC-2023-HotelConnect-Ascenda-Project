document.addEventListener("DOMContentLoaded", function () {
    //let hotelsDataJson; // Store the fetched hotels data

    // Get the current URL
    //const currentURL = window.location.href;

    // Create a URL object from the current URL
    //const url = new URL(currentURL);

    //const DestinationUid = url.searchParams.get("destination_id");//window.selectedDestinationUid;//store.get('DestinationUID');
    // Function to fetch hotels data from the API
    function fetchHotelsData() {
        alert('done writinggggg');
    }
    //requestHotelAPI();
    // Fetch the hotels data when the page loads
    fetchHotelsData();
});